{
  "name": "screenstart-marketing-trigger-submission",
  "version": "1.0.0",
  "description": "Lambda that broadcasts a shared video + AI doc to a contact segment via SES workers.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-lambda": "^3.600.0"
  }
}
